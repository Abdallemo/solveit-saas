{
  "$schema": "https://schema.railpack.com",
  "provider": "go",
  "steps": {
    "build": {
      "commands": [
        "GOOS=linux GOARCH=amd64 go build -ldflags='-s' -o=./out/linux_amd64 ./cmd"
      ]
    }
  },
  "deploy": {
    "startCommand": "./out/linux_amd64"
  }
}
